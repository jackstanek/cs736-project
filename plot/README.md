# Miss rate curve plotting

Tools for plotting miss rate curves.

To install in a virtualenv:

```
$ python3 -m venv ./venv
(venv) $ . venv/bin/activate
(venv) $ pip install -e .
```

To run:

```
(venv) $ plot <path_to_mrc_directory>
```
